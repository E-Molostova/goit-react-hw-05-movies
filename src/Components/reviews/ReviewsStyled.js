import styled from 'styled-components';

export const ReviewsContainer = styled.ul`
  list-style: none;
  padding: 0;
  margin: 0;
  .authorReview {
    font-weight: 500;
  }
  .noComment {
    margin-left: 20px;
  }
`;
